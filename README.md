# LocatorX
LocatorX turns any web page into a clean set of ready-to-use test locators. You paste a URL, the tool opens the page in a real browser, finds the elements a user can actually see, asks an AI to write the best locator for each one, verifies every locator live on the page, and gives you a results table plus an POM )file for Playwright or Selenium.
